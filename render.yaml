
services:
  - type: web
    name: agence-voyage
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    plan: free
    branch: main
    autoDeploy: true
    